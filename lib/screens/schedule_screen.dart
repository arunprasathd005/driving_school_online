import 'package:flutter/material.dart';

class ScheduleScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Schedule/Booking'),
      ),
      body: Center(
        child: Text('Schedule/Booking Screen'),
      ),
    );
  }
}
